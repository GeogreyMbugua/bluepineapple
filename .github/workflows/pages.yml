name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: https://bluepineapple.pages.dev
      NEXT_PUBLIC_APP_URL: https://bluepineapple.pages.dev
      NEXT_PUBLIC_GOOGLE_MAPS_KEY: AIzaSyDummyKeyForMapsValidation123
      NEXT_PUBLIC_ENVIRONMENT: production
      NEXT_PUBLIC_ENABLE_ANALYTICS: false
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Generate Prisma client
        run: pnpm --filter @blue-pineapple/database run generate
      - name: Build web app
        run: pnpm --filter @blue-pineapple/web build
      - uses: actions/upload-pages-artifact@v4
        with:
          path: apps/web/out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/deploy-pages@v4
        id: deployment
